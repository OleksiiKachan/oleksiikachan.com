version: 2
updates:
  - package-ecosystem: 'npm'
    directory: '/'
    schedule:
      interval: 'daily'
      time: '09:00'
      timezone: 'America/Toronto'
    groups:
      react:
        patterns:
          - 'react'
          - 'react-dom'
          - '@types/react'
          - '@types/react-dom'
      testing-library:
        patterns:
          - '*testing-library*'
      jest:
        patterns:
          - '*jest*'
      eslint:
        patterns:
          - '*eslint*'
        exclude-patterns:
          - 'eslint-config-next'
          - 'eslint-config-prettier'
          - 'eslint-plugin-prettier'
      prettier:
        patterns:
          - '*prettier*'
      next:
        patterns:
          - 'next'
          - 'eslint-config-next'
